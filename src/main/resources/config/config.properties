# === Global Configuration ===
# These values can be overridden by environment-specific properties
# or by System Properties (-Dbrowser=chrome)

browser=chrome
environment=dev
headless=false

# Retry
retry.max.count=1

# Timeouts (seconds)
explicit.wait=10
page.load.timeout=30
